const API_BASE = import.meta.env.VITE_API_URL || '/api';
const TOKEN_KEY = 'mindvault_token';
export const storage = { get token(){return localStorage.getItem(TOKEN_KEY)}, set token(v){v?localStorage.setItem(TOKEN_KEY,v):localStorage.removeItem(TOKEN_KEY)} };
async function request(path,{method='GET',body,headers={}}={}){const opts={method,headers:{...headers}};if(storage.token)opts.headers.Authorization=`Bearer ${storage.token}`;if(body instanceof FormData){opts.body=body}else if(body){opts.headers['Content-Type']='application/json';opts.body=JSON.stringify(body)}const res=await fetch(`${API_BASE}${path}`,opts);const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.message||'Request failed');return data}
export const api={register:(payload)=>request('/auth/register',{method:'POST',body:payload}),login:(payload)=>request('/auth/login',{method:'POST',body:payload}),me:()=>request('/auth/me'),stats:()=>request('/workspace/stats'),activity:()=>request('/workspace/activity'),search:(q)=>request(`/workspace/search?q=${encodeURIComponent(q)}`),notes:()=>request('/notes'),saveNote:(payload,id)=>request(id?`/notes/${id}`:'/notes',{method:id?'PUT':'POST',body:payload}),deleteNote:(id)=>request(`/notes/${id}`,{method:'DELETE'}),files:()=>request('/files'),upload:(form)=>request('/files',{method:'POST',body:form}),deleteFile:(id)=>request(`/files/${id}`,{method:'DELETE'}),ideas:()=>request('/ideas'),saveIdea:(payload,id)=>request(id?`/ideas/${id}`:'/ideas',{method:id?'PUT':'POST',body:payload}),deleteIdea:(id)=>request(`/ideas/${id}`,{method:'DELETE'}),productivity:()=>request('/productivity'),saveProductivity:(payload)=>request('/productivity',{method:'PUT',body:payload})};
